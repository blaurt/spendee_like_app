/**
 * Stores fixed url for each page
 * Used to avoid hardcoded links across whole codebase
 *
 * @type {{home: string, newRecord: string, record: string, recordList: string, category: string, categories: string, details: string, about: string}}
 */
export const Links = {
    home: '/',
    newRecord: '/new-record',
    records: '/records',
    categories: '/categories',
    details: '/details',
    about: '/about'
};